platform :ios, '10.0'

inhibit_all_warnings!
use_frameworks!

target 'CardNavigationStack' do
    pod 'AWPercentDrivenInteractiveTransition'
    pod 'Reveal-SDK', :configurations => ['Debug']
    pod 'pop', '~> 1.0'

end
